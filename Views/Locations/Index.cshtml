@model IEnumerable<DatabaseMgmt.Views.Location>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.aisleNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.shelfNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.section)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.aisleNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shelfNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.section)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.locationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.locationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.locationID })
        </td>
    </tr>
}

</table>
